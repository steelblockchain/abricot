import CustomStream from "./stream";
import ReaderStream, {
    ReaderBigEndianStream,
    ReaderLittleEndianStream,
} from "./reader";

export default {
    CustomStream,
    ReaderStream,
    ReaderBigEndianStream,
    ReaderLittleEndianStream,
};
